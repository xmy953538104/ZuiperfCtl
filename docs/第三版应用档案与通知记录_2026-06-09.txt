ZuiperfCtl 第三版应用档案与通知记录 - 2026-06-09

本轮改动:
- versionCode=3, versionName=0.3.0。
- App 改为平板双栏: 应用列表 + 应用档案详情。
- 应用列表来自 PackageManager launcher 应用，可搜索、勾选。
- 每个应用档案包含刷新率规则、XML 调度档案、AsoulOpt 线程档案。
- 新增常驻静音通知 PerfCtlQuickService，通知栏可切换 60/90/120/144Hz 或恢复默认。
- 新增 BootReceiver，开机尝试拉起通知服务。
- daemon 新增 /data/local/tmp/zui_perfctl/profiles/apps.prop。
- daemon 新增 set_app_profile/remove_app_profile。
- RefreshRateProvider 桥继续保持移除状态。

边界:
- 刷新率档案已闭环并会同步到 refresh/rules.prop。
- XML/线程档案已持久化，但 XML 生成器和 AsoulOpt 配置生成器还没实现，不伪装成已生效。

验证:
- bash -n zui_perfctld 通过。
- assembleDebug 通过。
- 临时测试 keystore assembleRelease 通过。
- aapt 确认 APK 为 versionCode=3/versionName=0.3.0，包含 PerfCtlQuickService/BootReceiver，不含旧 provider。
